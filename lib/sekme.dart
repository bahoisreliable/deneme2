import 'package:flutter/material.dart';

class Sekme extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Container(
          child: Text(
            "Baba Meraba",
            style: TextStyle(fontSize: 48),
          ),
        ),
      ),
    );
  }
}
